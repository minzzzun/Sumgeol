import SwiftUI

// MARK: - Theme
struct Theme {
    static let background = Color(hex: "#2D3436")      // 딥 네이비
    static let primary    = Color(hex: "#7ED6C1")      // 티파니 민트
    static let accent     = Color(hex: "#A29BFE")      // 라벤더 퍼플
    static let subtleText = Color.white.opacity(0.8)
}

// MARK: - Constants
struct AppConstants {
    static let appTitle = "숨결 (SumGyeol)"
}
